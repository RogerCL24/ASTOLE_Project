pandas>=2.2.0          # Procesamiento de datos (Ingeniero 1)
pyarrow>=14.0.0        # Parquet / IP2Location optimizado (ultra rápido)
scikit-learn>=1.4.0   # Normalización (Ingeniero 1)
langchain>=0.3.0      # Orquestación de agentes (Ingeniero 2)
langgraph>=0.2.0      # Workflow graph runtime
langchain-community>=0.3.0
chromadb>=0.5.0       # Base de datos vectorial (Ingeniero 3)
pysqlite3-binary; platform_system != "Windows" # Necesario para ChromaDB en Linux
streamlit>=1.35.0     # Interfaz de usuario (Ingeniero 4)
python-dotenv>=1.0.0  # Gestión de APIs
requests>=2.32.0      # Llamadas a APIs externas (CVE-2024-35195 patched in 2.32)
httpx>=0.27.0         # Cliente HTTP async para RAG
sentence-transformers>=3.0.0  # Embeddings de texto
torch>=2.3.0          # PyTorch runtime (install appropriate wheel for your platform if needed)
fastapi>=0.115.0      # API de triage y RAG externo
uvicorn[standard]>=0.30.0  # Servidor ASGI
litellm>=1.40.0       # Proxy unificado de modelos
langsmith>=0.1.0      # Trazabilidad y observabilidad
pytest>=8.0.0         # Tests
pytest-asyncio>=0.23.0  # Tests async
pyyaml>=6.0.1         # YAML frontmatter parsing — 6.0.1 patches CVE-2022-42969
ollama>=0.2.0         # Cliente Python para Ollama (opcional, se usa requests directamente)